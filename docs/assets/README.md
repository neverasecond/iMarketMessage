# iMM v0.1.0-alpha 截图

- `imm-demo-hero.png`：README 主截图（1280×700），iMM 规则列表与详情窗格同屏显示；侧栏展示脱敏的“VIX 高波动（示例）”及冷却信息，详情展示 VIX `>= 25` 条件与“演示模式 · 只读”提示。

截图均来自 `--demo-screenshot` 实际运行界面；主截图裁切为仅含 App 窗口的 1280×700 PNG，无鼠标指针遮挡。演示模式不读取本机数据、不联网、不写入 outbox。
